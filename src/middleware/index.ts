// src/middleware/index.ts

import authenticateToken from './auth';

export {
  authenticateToken,
  // Export other middleware here as needed
};
//check it aina
//export { default as authenticateToken } from './auth.middleware';
